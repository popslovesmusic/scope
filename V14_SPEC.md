
# V14 SPEC (condensed)

- Signature is primary state
- Corridor derived from peaks
- Orientation operators drive transformation
- Reasoning = iterative writeback
- Tokens are summaries

Update:
signature_next = (1 - ?) * signature + ? * gated_projection - ? * boundary_penalty
